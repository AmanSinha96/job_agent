# Job Automation Agent — Python dependencies
# Install: pip install -r requirements.txt
# Then: playwright install chromium

# ── Core automation ────────────────────────────────────────────────────────────
playwright>=1.44.0               # Browser automation (stealth config built-in)
schedule>=1.2.2                  # 6-hourly job scheduling

# ── Scraping ───────────────────────────────────────────────────────────────────
httpx>=0.27.0                    # Async HTTP (RSS feeds, NopeCHA polling)
beautifulsoup4>=4.12.0           # HTML/RSS parsing
lxml>=5.2.0                      # Fast XML/HTML parser backend for BeautifulSoup
python-jobspy>=1.1.80            # Indeed/LinkedIn/ZipRecruiter/Glassdoor/Google/Naukri scraping

# ── LLM (free Groq API, Gemini fallback) ────────────────────────────────────────
groq>=0.9.0                      # Groq Python SDK (llama-3.3-70b-versatile, free tier)
google-genai>=0.5.0               # Gemini fallback when Groq is rate-limited/down

# ── PDF generation (tailored resumes) ──────────────────────────────────────────
reportlab>=4.2.0                 # Primary PDF engine (lightweight, no system deps)
# weasyprint>=62.0               # Optional fallback — needs system libs (Pango, Cairo)
#                                 # Uncomment if reportlab output isn't styled enough.

# ── Gmail API ─────────────────────────────────────────────────────────────────
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# ── Standard lib only (no extra installs needed for):
# sqlite3, asyncio, re, json, logging, time, datetime, hashlib, pathlib, email, random
