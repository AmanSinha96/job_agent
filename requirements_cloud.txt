# Cloud dependencies (GitHub Actions runner)
# Most scraping runs on python-jobspy (HTTP-based, no browser). Naukri is the
# exception — jobspy's own Naukri scraper hits a 406 every time, so
# naukri_playwright.py drives a real browser instead (confirmed live, ~75%
# success rate, inherent to Naukri's bot detection — see that module).

httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
groq>=0.9.0
google-genai>=0.5.0
reportlab>=4.2.0
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
python-dotenv>=1.0.0
python-docx>=1.2.0
schedule>=1.2.2
python-jobspy>=1.1.80
playwright>=1.44.0
playwright-stealth>=2.0.0
